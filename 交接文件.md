# 核銷小幫手 — 系統管理與交接文件

> 這份文件是**給「接手維護這套系統的人」看的**，不是給日常上傳單據的同事看的。
> 日常操作（怎麼上傳、主管怎麼審核、財務怎麼填付款日期）請看 [README.md](README.md)。
>
> 讀這份文件的人，預期會需要：登入 Google Apps Script 改設定、重新部署、處理故障。
> 不需要會寫程式，但要能照著步驟操作 Google 的介面。

最後更新：2026-08（由建立者交接時整理）

---

## 0. 三十秒版本：這套系統長怎樣

- **前端網頁**（同事打開來上傳單據的網站）：一堆靜態檔案，放在 GitHub，由 Vercel 自動部署成公開網址。改網頁 = 改 GitHub 上的檔案，Vercel 幾分鐘內自動更新。
- **後端**：不是傳統伺服器，而是一段掛在 Google 試算表裡的 **Google Apps Script**。它負責把資料寫進試算表、把憑證存進 Google Drive、發 Slack 通知。
- **資料**：全部在 **Google 試算表 + Google Drive**，都在 service 信箱名下（見第 1 節）。
- **通知**：透過 Slack Incoming Webhook。

```
同事的瀏覽器
   │  上傳單據
   ▼
前端網頁（GitHub → Vercel 部署的公開網址）
   │  POST（帶 SECRET_TOKEN）
   ▼
Google Apps Script（掛在「總表」試算表裡）
   ├─ 寫一列進「收支總表」分頁
   ├─ 把憑證存進 Google Drive 的「核銷小幫手」資料夾
   ├─ 複製一列到該專案的「審核表」（獨立試算表）
   └─ 緊急件立刻發 Slack
```

---

## 1. 帳號歸屬（離職時最會出事的地方）

| 東西 | 放在哪／誰擁有 | 離職風險 |
|---|---|---|
| Google 試算表（總表）、Drive 憑證、各專案審核表 | **service 信箱**名下的共用雲端硬碟 / Drive | 低（已從個人帳號轉走） |
| Apps Script（後端程式） | 綁在總表試算表上，**執行身分 = service 信箱** | 低（已轉為 service 身分執行） |
| 四個機密設定值（見第 2 節） | 只存在 Apps Script 編輯器裡 + 你們的密碼保管處 | 中（只有知道的人才能重建，務必有第二人保管） |
| GitHub 儲存庫 | `github.com/SkillsforU/skills-for-u-receipt-helper` | ⚠️ **這是個人帳號 `SkillsforU`，不是 GitHub 組織** |
| Vercel 專案（前端部署） | 連著上面的 GitHub 帳號 | ⚠️ 同上 |

**還沒收尾的兩件事**（建議找時間處理，但不影響目前運作）：
1. GitHub 建一個真正的 Organization，把儲存庫轉過去，加第二位管理員。
2. Vercel 專案轉到組織 team 帳號。

> 為什麼 Google 那邊已經安全、GitHub/Vercel 還沒：Google 是資料，關掉個人帳號資料會存取不到，所以優先轉；GitHub/Vercel 只是程式碼跟部署，就算原帳號關了，程式碼在別人電腦上還有、網站也還在跑，頂多是「沒人能改」，比較不急。但長期還是要轉，才不會哪天要改東西卻沒人進得去。

---

## 2. 四個機密設定值

在 Apps Script 編輯器，`Code.gs` 最上方的「機密設定區」：

| 常數 | 用途 | 留空的話 |
|---|---|---|
| `SECRET_TOKEN` | 前端跟後端之間的共用密碼，防止別人亂寫資料 | 一定要設，否則等於沒鎖 |
| `GEMINI_API_KEY` | 雲端 OCR（Gemini）辨識用 | 留空＝只用離線辨識，不影響其他功能 |
| `SLACK_WEBHOOK_URL` | Slack 通知要送到哪個頻道 | 留空＝完全不發 Slack，其他功能正常 |
| `DRIVE_FOLDER_ID` | 憑證與審核表要存到哪個 Drive 資料夾 | 留空＝自動在「我的雲端硬碟」建一個，**正式環境建議指定共用雲端硬碟的資料夾** |

**這四個值請另外存一份在你們的密碼管理工具或只有管理者看得到的文件裡。**
它們只存在 Apps Script 編輯器裡，沒有別的備份——一旦要重貼程式碼、或換人接手，就得靠這份備份填回去。

⚠️ **絕對不要把這四個值寫進 GitHub 上的程式碼。** GitHub 儲存庫是公開的，寫進去等於公開外流。程式碼裡這幾行永遠保持空白／範例值，真正的值只在 Apps Script 編輯器裡手動填。

---

## 3. 怎麼改東西、怎麼部署（最常做的事）

### 3-1　改前端網頁（欄位、文字、版面）

前端檔案在 GitHub 儲存庫：`index.html`、`style.css`、`app.js`。

- 小改動：直接在 GitHub 網站上編輯檔案 → 存檔（commit）→ Vercel 幾分鐘內自動重新部署，公開網址就更新了。**不需要碰 Apps Script。**
- 判斷準則：如果你改的是「同事在網頁上看到/操作的東西」，通常只要改前端、不用動 Apps Script。

### 3-2　改後端（Apps Script）— 這是最容易出錯的一步

只要改到 `google-sync/Code.gs` 的內容，就要重新部署，步驟務必照做：

1. **用 service 信箱登入**（建議開無痕視窗，只登 service 信箱一個帳號，避免帳號搞混——見第 5 節）
2. 打開總表 →「擴充功能」→「Apps Script」
3. 把 [GitHub 上最新版 `google-sync/Code.gs`](https://github.com/SkillsforU/skills-for-u-receipt-helper/blob/main/google-sync/Code.gs) 整份複製，覆蓋編輯器裡的舊內容
4. **把第 2 節那四個機密值填回去**（貼上來的是公開版本，那幾格是空的）
5. 存檔（Ctrl/Cmd + S）
6. 右上「部署」→「**管理部署作業**」→ 點鉛筆（編輯）→ 版本欄選「**新版本**」→「部署」

> ⚠️ **第 6 步的「新版本」最關鍵。** 只存檔、沒選新版本的話，對外執行的還是舊程式碼——你會覺得「明明改了怎麼沒效」。這是最常見的踩雷點。
>
> 用「管理部署作業 → 編輯 → 新版本」而不是「新增部署作業」，這樣**網址不變**，前端跟同事那邊都不用重設。

### 3-3　異動人員或專案（不用改程式碼）

直接編輯總表的「**人員設定**」「**專案設定**」「**預算項目設定**」三個分頁，改完點總表選單「核銷小幫手」→「① 建立/更新設定與審核表」即可。**不用改程式碼、不用重新部署。**

- **人員設定**：`姓名`｜`Email`｜`Slack個人ID`
- **專案設定**：`專案名稱`｜`審核人Email（逗號分隔）`｜`狀態`（進行中／已結束）｜`憑證資料夾ID`｜`審核表ID（自動產生，勿改）`｜`審核表連結`
- **預算項目設定**：`專案/中心`｜`預算類別`｜`預算項目`｜`對應會計科目`。`專案/中心` 要跟「專案設定」的專案名稱一字不差，否則那個專案的預算項目下拉會是空的（不會報錯，是最容易安靜出錯的地方）。這張表**不用**改程式碼、也不用重跑選單——網頁開啟時會自動抓最新的。
- 新增專案：加一列 → 點「① 建立/更新設定與審核表」→ 系統自動建該專案審核表、設好權限。
- 專案結束：狀態改「已結束」→ 上傳選單不再出現、Slack 跳過，但資料完整保留。

---

## 4. 總表選單「核銷小幫手」各項功能

在總表上方選單列，跑 Apps Script 的 `onOpen` 產生（打不開就重新整理總表）：

| 選單項目 | 作用 |
|---|---|
| ① 建立/更新設定與審核表 | 讀「專案設定」，幫每個進行中專案建立/更新審核表與權限，**也會確認每份審核表上都裝著退件即時通知的觸發條件**（見下）。改完設定分頁後要點這個。 |
| ② 設定自動排程 | 安裝定時任務（見下）。第一次設定、或重新部署後排程不見時點。 |
| 立即同步審核結果 / 付款日期 | 手動把各審核表的審核結果、總表的付款日期同步。急件不想等排程時用。 |
| 立即發送待審提醒到 Slack | 手動發一次「今天是審核日」提醒。 |
| 💰 立即發送付款通知到 Slack | 財務填完付款日期後，手動發本月已付款彙總。**這個刻意不排程**（財務排班制，不一定在發款當天上班）。 |

**自動排程**（②裝的，裝在總表上，`ScriptApp.getProjectTriggers()` 裡看得到）：
- 每天約 23:00 同步審核結果與付款日期（跑在 Google 伺服器上，**電腦不用開著**）。
- 每天約 10:00 檢查是否為當月審核日（`DIGEST_DAY_OF_MONTH`，預設 10 號，遇週末順延到週一），是就發 Slack 提醒。

**還有一種觸發條件，不是②裝的，是①順便裝的**：每份專案審核表上都裝了一個「一被改成已退回就立刻通知申請人」的觸發條件（`onReviewStatusEdit_`）。這個是裝在**各審核表自己身上**，不是裝在總表上，所以在總表的觸發條件列表裡看不到——要查的話得個別打開每份審核表的「觸發條件」面板看。①每次執行都會確認這個有裝好、不會重複裝，所以正常不用管；如果退件通知突然沒作用了，先重跑一次①。

---

## 5. 踩過的坑（接手前務必看，都是真的發生過的）

這幾個都不是「程式壞了」，而是 Google 平台的細節行為，第一次遇到會很困惑。先看過，之後遇到就不會慌。

1. **帳號搞混 → 打不開 Apps Script / 檔案**
   症狀：點 Apps Script 出現「很抱歉，目前無法開啟這個檔案」。
   原因：瀏覽器同時登入了你的個人帳號跟 service 信箱，Google 用錯身分去開。
   解法：**開無痕視窗，只登 service 信箱一個帳號**再操作。

2. **改了 Code.gs 卻沒生效**
   原因：部署時沒選「新版本」（見 3-2 第 6 步）。

3. **新資料跑到第 1000 多列，不是第 2 列**（已修，但原理要知道）
   原因：早期版本用 `insertCheckboxes()` 對整欄設勾選框，會把 1000 列都寫入隱藏的值，害系統以為那些列「有資料」。現在改成每列單獨設定。若又出現類似錯位，先懷疑是不是有指令把整欄範圍寫了值。

4. **審核表被刪掉後，新單據寫不進去 / 系統報「在垃圾桶裡」**
   原因：Google 的「丟到垃圾桶」不是「永久刪除」，程式仍打得開垃圾桶裡的舊審核表。系統會**刻意報錯**要你決定，而不是自動重建（避免誤刪時新舊兩份並存）。
   解法：去垃圾桶把它復原；或去垃圾桶**永久刪除**它，再把「專案設定」該列的「審核表ID／連結」清空，重點一次「① 建立/更新設定與審核表」讓它重建。

5. **付款日期打「8/14」沒被算到**
   原因：付款日期是財務手打的，只打月/日沒年份，系統補不出來。
   現況：已加自動補年份（打完會變成 `2026-08-14`），但**只對「補上功能之後才編輯的」儲存格生效**，補上之前打的舊資料要重新編輯一次（點該格按 Enter 也算編輯）才會補。

6. **審核狀態被網頁蓋回「待審核」**（已修）
   原因：早期網頁的「重新同步」按鈕會把本機舊資料推回總表，蓋掉主管剛審好的結果。現在審核狀態只能「雲端→網頁」單向流動，網頁已無法反向覆寫。

7. **憑證存不進去 / 審核表沒建出來，但沒有錯誤**
   常見原因：service 信箱這個身分還沒對 Google Drive 完成授權。
   解法：在 Apps Script 編輯器，函式下拉選 `testDriveAuth` → 執行 → 跳出授權畫面時按「允許」。（同理，Gemini 授權用 `testGeminiAuth`。）

8. **本機儲存空間滿 → 送出看起來沒反應**（已修）
   原因：憑證縮圖存在瀏覽器本機，容量有限會爆。現在會自動清理已同步紀錄的縮圖並提示，不會再默默失敗。

9. **為什麼退件通知是用 `Utilities.sleep()` 等 1 分鐘，不是排程延後執行**
   這是刻意的取捨，不是想不到更好的做法。Apps Script 每個專案最多只有 **20 個觸發條件**；如果改成「建立一個延後執行的一次性觸發條件、執行完自己刪除」，理論上能更精準地等到審核人打完原因，但這個一次性觸發條件如果哪次因為程式出錯而沒刪乾淨，就會越堆越多，一旦爆表**會讓包括每天的審核同步在內的所有排程整個失效**，而且不會有任何警示。用 `sleep()` 換取「這個功能永遠不會多佔用任何觸發條件」，代價是如果審核人打退回原因花超過這段等待時間（`REJECT_NOTIFY_DELAY_MS`），那次通知的原因欄可能還是空的。之後如果真的要做到更精準，要先想好怎麼保證一次性觸發條件不會累積失控。

---

## 6. Slack 通知怎麼換頻道

Incoming Webhook 綁死在建立時選的頻道，換頻道要重新申請一組網址：

1. Slack →「Apps」→ 搜尋「Incoming Webhooks」→ 選新頻道 →「Add to Slack」→ 複製新網址
   （或到 https://api.slack.com/apps 建 App、開 Incoming Webhooks、Add New Webhook to Workspace）
2. 把新網址填進 Code.gs 的 `SLACK_WEBHOOK_URL` → 存檔 → 重新部署新版本。

⚠️ 若用**私人頻道**，`@` 提及只對「已經是該頻道成員」的人有效，要先把審核人邀進頻道。

---

## 7. 出事時的排查順序

1. **同事說上傳沒反應** → 先看網頁「上傳紀錄」該筆的「☁ 同步」狀態，會顯示失敗原因。多半是本機空間滿（已自動處理）或雲端同步設定沒設好。
2. **資料沒進總表** → 確認 Apps Script 是不是有部署新版本；確認 `SHEET_NAME` 跟總表分頁名一致（目前是「收支總表」）。
3. **審核結果沒同步** → 手動點選單「立即同步審核結果」；若報「垃圾桶」錯誤，見第 5 節第 4 點。
4. **要看後端到底噴什麼錯** → Apps Script 編輯器左側「執行項目」看紀錄（注意：用不同帳號檢視 Web App 執行紀錄可能被限制，要用 service 信箱看）。
5. **完全卡住** → GitHub 上有完整程式碼歷史，每個修正都有 commit 說明，可回溯任何一個版本。

---

## 8. 相關連結一覽

| 項目 | 位置 |
|---|---|
| 程式碼儲存庫 | https://github.com/SkillsforU/skills-for-u-receipt-helper |
| 後端程式（要貼進 Apps Script 的） | `google-sync/Code.gs` |
| 使用說明（給日常同事） | [README.md](README.md) |
| 前端公開網址 | https://skills-for-u-receipt-helper.vercel.app/|
| 總表試算表 | https://docs.google.com/spreadsheets/d/13WKP764RsWJdBFs-yGvDdkQzbzqMzrnzZ6BWNb9__Rk/edit?gid=358993791#gid=358993791|
| service 信箱 | service@skillsforu.org|
| 四個機密值保管處 | （請填入你們的密碼管理工具位置）|

> 上面幾個「請填入」的項目，交接時麻煩補上實際位置，接手的人才找得到。
